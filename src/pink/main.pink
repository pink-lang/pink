import libc {...}
import std {...}
import std.os {...}

#always

var flag_verbose: bool;
var flag_lazy: bool;
var flag_nosourcemap: bool;
var flag_notypeinfo: bool;
var flag_fullgen: bool;

func main(argc: int, argv: char const**): int {
    // main_test();
    return pink_entry(argc, argv, gen_all, "c");
}
